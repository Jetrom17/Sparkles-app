name: Android CI

on:
  push:
    branches: [ "**" ]
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches: [ "**" ]
    paths-ignore:
      - '**/*.md'
  workflow_dispatch:

jobs:
  init:
    name: Init
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Notify Telegram
        if: success()
        run: |
          curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -F chat_id="-1002327390277" \
            -F text="Application is being sent by @sparkleside" \
            -F message_thread_id="16"

  build_debug_apk:
    name: Build App Debug APK
    runs-on: ubuntu-latest
    needs: init  # Garante que o init seja executado primeiro

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'oracle'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Build with Gradle
        id: gradle_build_debug
        run: ./gradlew assembleDebug 

      - name: List build outputs
        run: ls app/build/outputs/apk/debug

      - name: Rename file 
        run: |
          mv app/build/outputs/apk/debug/app-debug.apk sparkles-app-debug.apk
          ls 

      - name: Send APK to Release Channel Telegram
        if: success()
        continue-on-error: true
        run: |
          if [ -f sparkles-app-debug.apk ]; then
            curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument" \
              -F chat_id="-1002327390277" \
              -F message_thread_id="16" \
              -F document=@"sparkles-app-debug.apk" \
              -F caption="@sparkleside"
          else
            echo "File sparkles-app-debug.apk not found!"
          fi
